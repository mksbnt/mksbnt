import { SecondsPostfixPipe } from './seconds-postfix.pipe';

describe('SecondsPostfixPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsPostfixPipe();
    expect(pipe).toBeTruthy();
  });
});
